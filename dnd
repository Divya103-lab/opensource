def a(b,c,arr):
    d=0
    e=0
    for f in arr:
        if f%c==0:
            e+=f
        else:
            d+=f
    return e-d
b,c=map(int,input().split())
arr=list(map(int,input().split()))
print(a(b,c,arr))
